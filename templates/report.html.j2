<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Network Backup Compliance Report - {{ scan_date }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-8">
    <div class="max-w-7xl mx-auto">
        <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-4">📋 Network Compliance Report</h1>
            <p class="text-gray-500">Generated: {{ scan_date }}</p>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-6">
                <div class="bg-blue-50 rounded-lg p-4 text-center">
                    <div class="text-3xl font-bold text-blue-600">{{ total_devices }}</div>
                    <div class="text-gray-600">Devices</div>
                </div>
                <div class="bg-green-50 rounded-lg p-4 text-center">
                    <div class="text-3xl font-bold text-green-600">{{ compliant }}</div>
                    <div class="text-gray-600">Compliant</div>
                </div>
                <div class="bg-red-50 rounded-lg p-4 text-center">
                    <div class="text-3xl font-bold text-red-600">{{ violations }}</div>
                    <div class="text-gray-600">Violations</div>
                </div>
                <div class="bg-yellow-50 rounded-lg p-4 text-center">
                    <div class="text-3xl font-bold text-yellow-600">{{ changes }}</div>
                    <div class="text-gray-600">Changes</div>
                </div>
            </div>
        </div>
        {% for device in devices %}
        <div class="bg-white rounded-lg shadow-lg p-6 mb-4">
            <div class="flex justify-between items-center mb-4">
                <h2 class="text-xl font-bold">{{ device.hostname }}</h2>
                <span class="px-3 py-1 rounded-full text-sm {{ 'bg-green-100 text-green-800' if device.compliant else 'bg-red-100 text-red-800' }}">
                    {{ 'COMPLIANT' if device.compliant else 'NON-COMPLIANT' }}
                </span>
            </div>
            {% if device.violations %}
            <table class="w-full">
                <thead class="bg-gray-100"><tr>
                    <th class="px-4 py-2 text-left">Rule</th>
                    <th class="px-4 py-2 text-left">Severity</th>
                    <th class="px-4 py-2 text-left">Description</th>
                </tr></thead>
                <tbody>
                {% for v in device.violations %}
                <tr class="border-t">
                    <td class="px-4 py-2">{{ v.name }}</td>
                    <td class="px-4 py-2">
                        <span class="px-2 py-1 rounded text-xs {{ 'bg-red-100 text-red-800' if v.severity == 'critical' else 'bg-yellow-100 text-yellow-800' }}">
                            {{ v.severity | upper }}
                        </span>
                    </td>
                    <td class="px-4 py-2 text-gray-600">{{ v.description }}</td>
                </tr>
                {% endfor %}
                </tbody>
            </table>
            {% endif %}
        </div>
        {% endfor %}
        <div class="text-center text-gray-500 mt-8 text-sm">
            Generated by Network Config Backup Tool | Author: Tamer Khalifa (CCIE #68867)
        </div>
    </div>
</body>
</html>
